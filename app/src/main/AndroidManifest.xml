<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elvishtranslator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElvishTranslator">
        <activity android:name=".MainActivity"></activity>
        <activity
            android:name=".phrase_Activity"
            android:label="Frases"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".color_Activity"
            android:label="Colores"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".family_Activity"
            android:label="Familia"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".number_Activity"
            android:label="Números"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".splash"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>